[package]
name = "iced-arrows"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { version = "0.14.0", features = ["canvas"] }
noise = "0.9.0"
poisson-diskus = "1.0.0"
vizkit = { path = "../.." }
