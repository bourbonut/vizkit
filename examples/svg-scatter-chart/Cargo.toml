[package]
name = "svg-scatter-chart"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.4.0"
serde = { version = "1.0.228", features = ["derive"] }
svg = "0.18.0"
vizkit = { path = "../.." }

[build-dependencies]
reqwest = { version = "0.13.2", features = ["blocking"] }
